// Simple Lox program for testing the scanner

var name = "Lox";
var version = 1.0;

fun greet(name) {
  print("Hello, " + name + "!");
}

greet(name);
print("Welcome to Lox version " + version + "\n");
var a == 5;